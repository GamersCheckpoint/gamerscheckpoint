<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Extraer Datos de Usuarios</title>
</head>
<body>
    <h1>Datos de Usuarios</h1>
    <hr>
    <form action="/extraerDatosUsuario" method="post">
        <label>Extraer Datos de Usuario</label>
        <input type="text" placeholder="Nombre de Usuario" id="nombre" name="nombre"/>
        <button type="submit">Extraer</button>
    </form>

    <ul>
    <% if(typeof datos !== 'undefined'){ %>
        <% for (index in datos){ %>
            <li><%= datos[index].nombre %> <%= datos[index].apellido %></li>
        <% } %>
    <% } %>
    </ul>


</body>
</html>